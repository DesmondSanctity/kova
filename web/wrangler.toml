name = "usekova"
compatibility_date = "2026-07-22"
pages_build_output_dir = "dist"

# Point this at your deployed Unikraft backend. Override per-environment in the
# Cloudflare dashboard (Settings → Variables) if you prefer not to commit it.
[vars]
API_BASE = "https://kova-api.fra.unikraft.app"
